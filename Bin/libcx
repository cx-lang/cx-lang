#!/usr/bin/env node

'use strict'

const { join } = require( 'path' )

let app = require( join( __dirname, '..', 'package-manager' ) )

if ( app.hasOwnProperty( 'default' ) ) app = app[ 'default' ]

app( process.cwd(), process.argv.slice( 2 ) )
